Installing Happy ABC on a Unix machine
======================================

PREREQUISITES
-------------

* ocaml 3.10 or latter
* omake
* GODI with jane-street-core extlib ounit


INSTALLATION INSTRUCTIONS
-------------------------

If you type:

   $ omake
   $ (sudo) omake install

then install to a default directory(/usr/local/bin).

Or,you want to install to another directory:

   $ omake    
   $ (sudo) omake install PREFIX=/path/to/

